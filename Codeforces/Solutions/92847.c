#include <stdio.h>

int main()
{
    int n, m;
    scanf("%d %d", &n, &m);

    int l, r, d;

    for (int i = 0; i < m; i++)
    {
        scanf("%d %d %d", &l, &r, &d);
    }

    


    return 0;
}